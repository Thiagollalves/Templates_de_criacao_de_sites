{
  "name": "static-site-template",
  "version": "1.0.0",
  "private": true,
  "description": "Template estatico reutilizavel para sites institucionais e landing pages.",
  "scripts": {
    "sync:content": "node scripts/sync-content.js",
    "dev:serve": "node scripts/dev-server.js",
    "dev": "npm run sync:content && npm run dev:serve",
    "start": "npm run sync:content && npm run dev:serve",
    "test": "node scripts/validate-template.js"
  },
  "keywords": [
    "template",
    "static-site",
    "landing-page"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
